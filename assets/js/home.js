/*
  This page relies on the global functions defined in common_script.js.
  - `navigateTo(targetPageId)` is used for all navigation actions.
  - `getTimeBasedGreeting()` and user data population for the welcome message
    are automatically handled by the common script on DOMContentLoaded.
  No additional page-specific JavaScript is required for this dashboard.
*/